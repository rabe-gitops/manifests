apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: app-vs
spec:
  hosts:
  - app.rabegitops.it
  gateways:
  - app-gateway
  http:
  - route:
    - destination:
        host: app-service-staging
        port:
          number: 8080